# Preserved Stillwater releases

The development journal is at [progression.html](../progression.html). The current experience remains at the project root. `versions/v1/` and `versions/v2/` preserve runtime bytes from commits `fc31a00f94992a84b681b94aac22ea724e2c7385` and `dc45fb2574d299dd03dab67da4ad7e44bd468709`, including their own relative dependencies and asset credits.

[archive.json](archive.json) records SHA-256 hashes for those runtime files and the four original screenshot PNGs per release. `photos/` contains WebP derivatives of the original screenshots for the journal. Source PNGs can also be retrieved from each original commit's `screenshots/` directory. These are actual captures; no separate intermediate scene versions are implied.

Do not edit a frozen version. Add a new version directory, source commit, screenshot set, and manifest entry for the next release. Preserve matching viewpoints and record the problem, change, evidence, and lesson. The journal's four working passes describe two preserved releases; some earlier working states were not captured separately.

The local Astra workspace maintains shared editorial sources and archive/build scripts in its parent project's `journal/` directory. The public HTML, stylesheet and JavaScript are self-contained static output and can also be maintained directly here. The public journal has no private chat dependencies.
